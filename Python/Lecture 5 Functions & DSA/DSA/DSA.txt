1.List:

1.Built-in dynamic array which can store elements of different Data types.
2.It is Ordered collection of item.elements are Stored in the same order as they are inserted
3.List stores references to the object (elements) rather than the actual data itself
Example: a=[10,20,'Divya',True]


2.String:

1.String is a sequenece of character enclosed within quotes a single('') or double (" ")
2.These are Immutable,once created than cannot be chnage
3.String can contain letters,numbers,and special characters.
4.They support a wide range of operations such as slicing,concatenation etc


3.Set:

1.set is a in-built function of a python that can stores unique elements of different data types
2.It is Unordered.Element dont maintain any specific order as they added.
3.Sets dont allows any duplicate elements and automatically removes duplicates.


4.Dictionary:

1.It is a mutable , unordered,collection of data that stores data in the form of key value pair.
2.It is like a hash map in the another language.
3.Each key is unique and immutable,the values associated with key can be any data type.
3.Disctionary can have two or more dictionary means dict in dict.

